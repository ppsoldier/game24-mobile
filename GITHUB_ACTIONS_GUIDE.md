# GitHub Actions 自动打包APK 使用指南

## 快速开始

### 第一步：创建GitHub仓库

1. 访问 https://github.com/new
2. 创建一个新的公开仓库
3. 仓库名建议：`game24-mobile` 或 `24点扑克牌游戏`
4. 不要初始化README、.gitignore或license（我们已经有这些文件了）

### 第二步：初始化本地Git仓库

在项目目录中打开终端，运行以下命令：

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add .

# 提交更改
git commit -m "Initial commit: 24点扑克牌游戏移动版"

# 重命名主分支为main（如果需要）
git branch -M main
```

### 第三步：连接到GitHub仓库

```bash
# 添加远程仓库（替换下面的URL为你的仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 推送代码到GitHub
git push -u origin main
```

如果遇到认证问题，可以使用SSH：

```bash
git remote set-url origin git@github.com:你的用户名/你的仓库名.git
```

### 第四步：等待自动构建

1. 推送代码后，GitHub Actions会自动开始构建
2. 访问你的GitHub仓库
3. 点击"Actions"标签查看构建进度
4. 构建大约需要10-20分钟

### 第五步：下载APK

1. 等待构建完成（显示绿色✓）
2. 点击构建任务进入详情页
3. 滚动到底部找到"Artifacts"部分
4. 点击"game24-apk"下载
5. 解压ZIP文件获得APK

## 手动触发构建

如果不想每次推送都自动构建，可以手动触发：

1. 进入仓库的"Actions"标签
2. 在左侧选择"Build Android APK"
3. 点击"Run workflow"按钮
4. 选择分支（通常是main）
5. 点击绿色的"Run workflow"按钮

## 创建版本发布

创建正式版本时：

```bash
# 创建标签
git tag v1.0.0

# 推送标签到GitHub
git push origin v1.0.0
```

然后在GitHub上：
1. 进入"Releases"页面
2. 点击"Create a new release"
3. 选择刚推送的标签v1.0.0
4. 填写发布标题和说明
5. 点击"Publish release"

APK会自动附加到Release中。

## 常见问题

### 1. 推送时提示认证失败

**解决方案**：
- 使用Personal Access Token（PAT）
- 访问 https://github.com/settings/tokens
- 生成新的token，勾选"repo"权限
- 使用token代替密码进行推送

### 2. 构建失败

**可能原因**：
- buildozer.spec配置错误
- 依赖包问题
- 网络问题

**解决方案**：
- 查看Actions日志中的错误信息
- 检查buildozer.spec文件配置
- 尝试重新触发构建

### 3. APK无法安装

**解决方案**：
- 确保Android版本 >= 5.0
- 启用"未知来源"安装
- 检查APK文件是否完整

### 4. 游戏运行崩溃

**解决方案**：
- 检查设备存储空间
- 重启设备
- 尝试重新安装APK

## 自定义配置

### 修改应用名称

编辑 `buildozer.spec` 文件：

```ini
[app]
title = 你的应用名称
```

### 修改包名

```ini
[app]
package.name = game24
package.domain = org.yourdomain
```

### 修改版本号

```ini
[app]
version = 1.0.0
```

### 添加权限

```ini
[buildozer.target.android]
android.permissions = INTERNET,WRITE_EXTERNAL_STORAGE
```

## 进阶：修改GitHub Actions配置

编辑 `.github/workflows/build-android.yml` 可以自定义构建流程：

- 修改触发条件
- 添加更多构建步骤
- 配置自动发布
- 添加测试步骤

## 获取帮助

- GitHub Actions文档：https://docs.github.com/en/actions
- Buildozer文档：https://buildozer.readthedocs.io/
- Kivy文档：https://kivy.org/doc/stable/

---

祝你打包顺利！🎉
